{
  "name": "webgpu-webgl-particle-demo",
  "version": "1.0.0",
  "description": "Interactive particle system comparing WebGPU and WebGL performance",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.66",
    "gh-pages": "^6.3.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
